#include <dlfcn.h>
#include <stdio.h>


/**
* The .so libloadable.so exports a single function:
* int exported(int). You will need to load the library,
* then find and call the export.
*/
int main(int argc, char** argv)
{

    return 0;
}
